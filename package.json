{
  "name": "wikiora",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @wikiora/web",
    "build:web": "npm run build --workspace @wikiora/web",
    "dev:api": "make api"
  }
}
